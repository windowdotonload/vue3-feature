/*
 * @Descripttion:
 * @version:
 * @Author: windowdotonload
 */
import { defineComponent } from "vue";

export default defineComponent({
    props: {

    },
    setup(props) {
        return () => {
            return (
                <h1>this is columnList of tsxs</h1>
            )
        }
    }
})
